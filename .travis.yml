language: r
r: bioc-devel

cache: packages

before_install:
  - tlmgr install tcolorbox
  - tlmgr install bera
  - tlmgr install nowidow
  - tlmgr install parnotes
  - tlmgr install marginfix

addons:
  apt:
    packages:
      - ghostscript

# safelist
branches:
  only:
  - master
  - development
